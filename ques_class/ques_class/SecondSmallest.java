package ques_class;

public class SecondSmallest {
    int [] a ={23,45,67,87,92};
    int smallest  = a[0]
    for (int i = 0 ; i < a.length ; i++){
        if  (smallest>a[i])
            { smallest = s[i]};
    }
    int SecondSmallest = -1;
    for (int i = 0 ; i < a.length ; i++){
        if  (smallest!=a[i] && )
            { smallest = s[i]};
    }
}
